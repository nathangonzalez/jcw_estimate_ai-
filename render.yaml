services:
  - type: web
    name: jcw-estimate
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app -c gunicorn.conf.py"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
